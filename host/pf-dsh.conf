# Wrapper ruleset loaded in place of /etc/pf.conf, which is never edited:
# Apple's stock rules verbatim, plus the dsh egress anchor.
include "/etc/pf.conf"

anchor "dsh-egress"
load anchor "dsh-egress" from "/etc/pf.anchors/dsh-egress"
